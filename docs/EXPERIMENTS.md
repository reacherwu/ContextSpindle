# Experiments

Every run writes `experiments/results/<experiment_id>/` containing `config.json`, `metrics.json`, `stdout.log`, `system.json`, and `report.md`. Results are immutable once recorded; corrections create a superseding run with a reason.

Important conclusions require three independent seeds and report mean, standard deviation, best, and worst values.
