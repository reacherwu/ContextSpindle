[package]
name = "diffhound-cli"
version = "0.1.0"
edition = "2021"
description = "DiffHound: Zero-Noise AI PR Guard & Anti-Regression Engine"
license = "AGPL-3.0"
authors = ["Reacher Wu <reacherwu@gmail.com>"]

[[bin]]
name = "diffhound"
path = "src/main.rs"

[dependencies]
continuum-core = { path = "../continuum-core" }
